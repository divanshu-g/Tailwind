npx tailwind init [anyname] --full

When to Use?
If you need to heavily customize Tailwind CSS (e.g., add custom colors, fonts, spacing).
If you want to understand the default Tailwind settings and tweak them.
